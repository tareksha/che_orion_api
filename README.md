# Orion API over Eclipse Che
This standalone web application is an adapter that processes Eclipse Orion API calls and maps them to Eclipse Che calls, forwarding the operations to an Eclipse Che server.
